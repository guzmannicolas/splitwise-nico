Next.js frontend scaffold for Splitwise clone.

Quick start:
1. cd frontend
2. npm install
3. add environment variables in a `.env.local`:
   NEXT_PUBLIC_SUPABASE_URL=your_url
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
4. npm run dev
